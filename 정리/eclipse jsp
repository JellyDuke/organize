~~.set~~(저장할파라미터);
~~.get~~(가져올파라미터);

ex) Member mem = new Member();
    mem.setMid(mid);
    mem객체에 저장되어있는(내가 정의한 Member class 혹은 파일 내에서) Mid에
    저장시킨다.

String memail = memailId + "@" + memailDomain;
mem.setMemail(memail);
문자열 memmail에 memailId변수 문자@ memailDomain을 저장시킴.
memail을 Member에 있는 memail을 파라미터를 넣음.
